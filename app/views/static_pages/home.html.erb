<h1> home </h1>

<% if !student_signed_in? && !tutor_signed_in? && !admin_signed_in? %>
<%= link_to "Admin Log In", new_admin_session_path %>
<br><%= link_to "Student Log In", new_student_session_path %>
<% end %>

<% if admin_signed_in? %>
	ADMIN IS SIGNED IN!
	<% end %>

<% if student_signed_in? %>
	STUDENT SIGNED IN!
	<% end %>


<% if tutor_signed_in? %>
	TUTOR SIGNED IN!
	<% end %>